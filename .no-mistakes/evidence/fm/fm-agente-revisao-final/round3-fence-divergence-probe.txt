===== O veredito que o gate registra não pode divergir do que o humano lê =====
Relatório: o parecer real do revisor, com um preâmbulo que cita o modelo da skill
dentro de um bloco de código cercado dizendo "Aprovado".

--- o que o humano lê no relatório ---
preâmbulo cercado (linhas 1-13):
# Revisão final independente — `preco_final`

**Objeto revisado:** `desconto.py` (função `preco_final`).

O modelo da skill abre a primeira seção assim:

```markdown
## Veredito

Aprovado
```

## Veredito
...
a seção '## Veredito' real que o humano lê:
**Não aprovado.** Trata-se de falha demonstrada, não de bloqueio por risco: o critério de aceite declarado na própria entrega foi reproduzido e falhou. `preco_final(200, 10)` retorna `-1800`, quando o aceite exige `180`.

--- cenário adversarial: o status tenta registrar 'Aprovado' ---
$ cat <status>
working: revisão final iniciada em desconto.py/ENTREGA.md (commit a4ef4bf)
done: revisão final Aprovado report=/private/tmp/fm-r3-live.XnOqvm/home/data/r3-rev-final/report.md

$ bin/fm-teardown.sh r3-rev-final
REFUSED: reviewer task r3-rev-final status does not match report verdict 'Não aprovado' and path /private/tmp/fm-r3-live.XnOqvm/home/data/r3-rev-final/report.md.
teardown exit=1

metadata da tarefa ainda presente: sim
relatório preservado: sim
janela tmux do revisor ainda aberta: 1
